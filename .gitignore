# Secrets & machine-local overrides — never tracked
*_tokens
*.local
/secrets/
.env
*.env

# SSH / key material (defensive; keys live in ~/.ssh, not here)
id_*
!id_*.pub
*.pem
*.key
*_ed25519
*_ed25519_sk

# OS / editor cruft
.DS_Store
*.swp
*~

# Local sync runtime (should never be inside the repo, but just in case)
.cache/
